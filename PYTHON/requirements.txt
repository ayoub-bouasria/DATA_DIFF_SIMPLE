# DATA DIFF PYTHON - Dependencies
# ================================
# Compatible with Python 3.13.7
# Using datacompy with Snowflake connector (pandas-based comparison)

# Core comparison library (without snowflake extra - uses pandas)
datacompy>=0.14.0

# Snowflake connector - INSTALL WITH PRE-BUILT WHEELS:
#   pip install --only-binary :all: snowflake-connector-python
# If that fails, use conda:
#   conda install -c conda-forge snowflake-connector-python
snowflake-connector-python[pandas]

# Data manipulation - Python 3.13 compatible versions
pandas>=2.2.0
numpy>=2.1.0

# Configuration management
python-dotenv>=1.0.0

# Excel export support
openpyxl>=3.1.0
xlsxwriter>=3.2.0

# CLI
click>=8.1.0

# YAML config support
pyyaml>=6.0.1

# ============================================================
# INSTALLATION NOTES FOR PYTHON 3.13
# ============================================================
# If pip tries to compile snowflake-connector-python, use:
#
#   pip install --only-binary :all: -r requirements.txt
#
# Or install via conda-forge:
#   conda install -c conda-forge snowflake-connector-python
#   pip install datacompy pandas openpyxl xlsxwriter click pyyaml python-dotenv
